extends layout

block content

  h1 #{title}

  form(method='POST')
    div.form-group
      label(for='genre') Genre:
      input#genre.form-control(type='text', placeholder='Fantasy, Poetry etc.' name='genre'  value=(undefined===genre ? '' : genre.name) )
    button.btn.btn-primary(type='submit') Submit

  if errors
    ul
      for error in errors
        li!= error.msg
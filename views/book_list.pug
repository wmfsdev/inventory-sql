extends layout

block content
  h1= title
  if book_list.length
    ul
      each book in book_list
        li
          a(href=`/books/${book.book_id}`) #{book.title}
          |  (#{book.family_name}, #{book.first_name})
  else
    p There are no books.